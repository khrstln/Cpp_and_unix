#ifndef EXECUTE_COMMANDS_HPP
#define EXECUTE_COMMANDS_HPP

#include <vector>
#include "Command.hpp"

// Функция executeCommands для последовательного выполнения команд
void executeCommands(const std::vector<Command*>& commands);

#endif
